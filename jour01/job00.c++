#include <iostream>
using namespace std;

int main() {
    int a, b, x, y, c, d, i, n, p; 

    a = x + 5;
    x = y + 2; 
    a = (x == y); 
    bool result = (a < b) && (c < d); 
    int product = (i++) * (n + p); 
}
